services:
  - type: web
    name: cartas-contra-humanidade
    env: node
    buildCommand: npm install
    startCommand: node src/server.js
    envVars:
      - key: NODE_ENV
        value: production
